name: pii_detection
model: null
response_format: |
  {
    "type": "object",
    "properties": {
      "contains_pii": {
        "type": "boolean"
      },
      "entities": {
        "type": "array",
        "items": {
          "type": "string",
          "enum": ["email", "phone", "address", "name", "other"]
        },
        "uniqueItems": true
      }
    },
    "required": ["contains_pii", "entities"],
    "additionalProperties": false
  }
instruction: Detect personally identifiable information in the supplied text.
parameters:
  max_completion_tokens: 100
  temperature: 0.0
sentence_boundaries: null
